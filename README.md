# AutoUmount

Auto unmounts a FUSE directory after it is not accessed after some time

Might be helpful if you always forgot to umount an encrypted filesystem (EncFS, GoCryptFs, etc.) after accessing it